const axios = require('axios');
async function run() {
  try {
    // we don't have session cookie so this will fail 401. Let's just create a test express server to check multer behavior.
  } catch(e) {}
}
